name: Deploy to GitHub Pages

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - name: Validate YAML data files
        run: |
          node -e "
          const yaml = require('js-yaml');
          const fs = require('fs');
          const p = yaml.load(fs.readFileSync('src/data/projects.yaml','utf8'));
          if(!p.projects || !Array.isArray(p.projects)) throw new Error('Invalid projects.yaml: missing projects array');
          const validCategories = ['agent','terminal','skills','governance'];
          const requiredFields = ['id','name','category','repo','description','scores','communityData','tags'];
          p.projects.forEach((pr,i)=>{
            requiredFields.forEach(k=>{if(pr[k]===undefined) throw new Error('Project '+i+' missing '+k)});
            if(!validCategories.includes(pr.category)) throw new Error('Project '+i+' invalid category');
          });
          console.log('  projects.yaml: '+p.projects.length+' projects valid');
          const d = yaml.load(fs.readFileSync('src/data/dimensions.yaml','utf8'));
          if(!d.dimensions || !Array.isArray(d.dimensions)) throw new Error('Invalid dimensions.yaml');
          console.log('  dimensions.yaml: '+d.dimensions.length+' dimensions valid');
          "

  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - name: Run smoke tests
        run: npm test

  build:
    needs: [validate, test]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - name: Type check
        run: npx astro check
      - name: Build site
        run: npm run build
      - name: Check no hardcoded base paths in built output
        run: |
          # Only flag paths that are NOT Astro-generated (href/src/prefetch with proper base prefix)
          if grep -r '/skills123\.cc[a-z]' dist/ --include='*.html' 2>/dev/null; then
            echo "ERROR: Found hardcoded base path (missing slash) in built HTML"
            exit 1
          fi
          echo "  No hardcoded base paths found"
      - uses: actions/upload-pages-artifact@v3
        with:
          path: dist

  deploy:
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
